# Test suite

This directory is for automated frontend tests and their fixtures.

Authoritative commands:

```sh
npm run test:all
npm run test:mocha
npm run test:vitest
```

Standalone debugging and audit scripts belong in `scripts/diagnostics/`, not in
this directory.
