# Repository Agent Notes

This repository contains the PhyloMovies application and a separate Markdown knowledge system.

## Application Code

For application changes, follow the existing project conventions in `README.md`, `CONTRIBUTING.md`, `package.json`, `src/`, `test/`, and `docs/`. This file does not replace those conventions.

## Knowledge Wiki

For work inside `knowledge/`, follow `knowledge/AGENTS.md`. The wiki protocol is scoped to source ingestion, knowledge synthesis, query answers, and wiki maintenance.

Do not modify files in `knowledge/sources/` during wiki synthesis unless the user explicitly asks to add or register a new raw source.
