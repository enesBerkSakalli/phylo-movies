# Helpers

This folder contains local convenience scripts for environment setup and data
download. They support reproducibility, but they are not the canonical analysis
method.

Before public release, verify all external URLs, access dates, checksums, and
genome-build assumptions.
