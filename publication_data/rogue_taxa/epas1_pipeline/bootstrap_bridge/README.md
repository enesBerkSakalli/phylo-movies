# Bootstrap Bridge

This folder contains scripts that connect selected external Aberer/RogueNaRok
style bootstrap inputs to Phylo-Movies-ready ordered tree files.

The external source data is not vendored here. Add only selected files under
`../../selected_external_files/` after provenance review.

Current status: potentially useful, but not publication-ready until paths and
datasets are made configurable.
